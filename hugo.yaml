baseURL: https://neurodatawithoutborders.github.io/
languageCode: en
title: Neurodata Without Borders

# Pagination
pagination:
  pagerSize: 10

# Markup
markup:
  goldmark:
    renderer:
      unsafe: true

# Modules
module:
  mounts:
    - source: assets
      target: assets
